
---
---

<section id="blog" class="py-20 transition-colors duration-300" :class="lightMode ? 'bg-white' : 'bg-gray-900'">
  <div class="container mx-auto px-4 sm:px-6">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-16 animate-slide-up">
        <h2 class="text-3xl sm:text-4xl font-bold mb-4 transition-colors duration-300" :class="lightMode ? 'text-gray-900' : 'text-white'">Technical Blog</h2>
        <p class="text-lg transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-400'">Insights and tutorials on DevOps best practices</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <!-- Article 1 -->
        <article class="group cursor-pointer animate-slide-up" onclick="openArticle('kubernetes-best-practices')">
          <div class="rounded-xl overflow-hidden border transition-all duration-300 transform group-hover:scale-105 group-hover:shadow-xl" :class="lightMode ? 'bg-white border-gray-200 shadow-lg' : 'bg-gray-800 border-gray-700'">
            <div class="h-48 bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center">
              <svg class="w-16 h-16 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
              </svg>
            </div>
            <div class="p-6">
              <div class="flex items-center mb-3">
                <span class="px-3 py-1 text-xs font-medium bg-blue-100 text-blue-800 rounded-full">Kubernetes</span>
                <span class="ml-3 text-sm transition-colors duration-300" :class="lightMode ? 'text-gray-500' : 'text-gray-400'">Jan 2025</span>
              </div>
              <h3 class="text-xl font-bold mb-3 transition-colors duration-300" :class="lightMode ? 'text-gray-900' : 'text-white'">Kubernetes Best Practices for Production</h3>
              <p class="text-sm leading-relaxed transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-400'">Essential guidelines for running Kubernetes clusters in production environments, including security, monitoring, and resource management.</p>
            </div>
          </div>
        </article>

        <!-- Article 2 -->
        <article class="group cursor-pointer animate-slide-up" style="animation-delay: 0.1s" onclick="openArticle('cicd-pipeline-optimization')">
          <div class="rounded-xl overflow-hidden border transition-all duration-300 transform group-hover:scale-105 group-hover:shadow-xl" :class="lightMode ? 'bg-white border-gray-200 shadow-lg' : 'bg-gray-800 border-gray-700'">
            <div class="h-48 bg-gradient-to-br from-green-500 to-teal-600 flex items-center justify-center">
              <svg class="w-16 h-16 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              </svg>
            </div>
            <div class="p-6">
              <div class="flex items-center mb-3">
                <span class="px-3 py-1 text-xs font-medium bg-green-100 text-green-800 rounded-full">CI/CD</span>
                <span class="ml-3 text-sm transition-colors duration-300" :class="lightMode ? 'text-gray-500' : 'text-gray-400'">Dec 2024</span>
              </div>
              <h3 class="text-xl font-bold mb-3 transition-colors duration-300" :class="lightMode ? 'text-gray-900' : 'text-white'">Optimizing CI/CD Pipelines</h3>
              <p class="text-sm leading-relaxed transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-400'">Strategies to reduce build times and improve deployment reliability in your CI/CD pipelines using modern tools and techniques.</p>
            </div>
          </div>
        </article>

        <!-- Article 3 -->
        <article class="group cursor-pointer animate-slide-up" style="animation-delay: 0.2s" onclick="openArticle('infrastructure-as-code')">
          <div class="rounded-xl overflow-hidden border transition-all duration-300 transform group-hover:scale-105 group-hover:shadow-xl" :class="lightMode ? 'bg-white border-gray-200 shadow-lg' : 'bg-gray-800 border-gray-700'">
            <div class="h-48 bg-gradient-to-br from-orange-500 to-red-600 flex items-center justify-center">
              <svg class="w-16 h-16 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
            </div>
            <div class="p-6">
              <div class="flex items-center mb-3">
                <span class="px-3 py-1 text-xs font-medium bg-orange-100 text-orange-800 rounded-full">IaC</span>
                <span class="ml-3 text-sm transition-colors duration-300" :class="lightMode ? 'text-gray-500' : 'text-gray-400'">Nov 2024</span>
              </div>
              <h3 class="text-xl font-bold mb-3 transition-colors duration-300" :class="lightMode ? 'text-gray-900' : 'text-white'">Infrastructure as Code with Terraform</h3>
              <p class="text-sm leading-relaxed transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-400'">Complete guide to managing cloud infrastructure using Terraform, including best practices for modules and state management.</p>
            </div>
          </div>
        </article>
      </div>
    </div>
  </div>
</section>

<!-- Blog Article Modal -->
<div id="blog-modal" class="fixed inset-0 bg-black bg-opacity-50 z-50 hidden">
  <div class="flex items-center justify-center min-h-screen p-4">
    <div class="max-w-4xl w-full max-h-screen overflow-y-auto rounded-xl border" :class="lightMode ? 'bg-white border-gray-200' : 'bg-gray-900 border-gray-700'">
      <div class="sticky top-0 flex justify-between items-center p-6 border-b" :class="lightMode ? 'bg-white border-gray-200' : 'bg-gray-900 border-gray-700'">
        <h2 id="modal-title" class="text-2xl font-bold transition-colors duration-300" :class="lightMode ? 'text-gray-900' : 'text-white'"></h2>
        <button onclick="closeBlogModal()" class="text-gray-500 hover:text-gray-700 transition-colors duration-300">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>
      <div id="modal-content" class="p-6 transition-colors duration-300" :class="lightMode ? 'text-gray-800' : 'text-gray-200'">
        <!-- Article content will be loaded here -->
      </div>
    </div>
  </div>
</div>

<script>
  const articles = {
    'kubernetes-best-practices': {
      title: 'Kubernetes Best Practices for Production',
      content: `
        <div class="prose prose-lg max-w-none">
          <h3>Introduction</h3>
          <p>Running Kubernetes in production requires careful planning and adherence to best practices. This guide covers essential considerations for maintaining secure, scalable, and reliable Kubernetes clusters.</p>
          
          <h3>Security Best Practices</h3>
          <ul>
            <li><strong>RBAC (Role-Based Access Control):</strong> Implement fine-grained permissions for users and service accounts</li>
            <li><strong>Network Policies:</strong> Control traffic flow between pods and namespaces</li>
            <li><strong>Pod Security Standards:</strong> Use security contexts and admission controllers</li>
            <li><strong>Secrets Management:</strong> Never store sensitive data in plain text</li>
          </ul>
          
          <h3>Resource Management</h3>
          <p>Proper resource allocation ensures optimal performance and cost efficiency:</p>
          <pre><code>apiVersion: v1
kind: Pod
spec:
  containers:
  - name: app
    resources:
      requests:
        memory: "256Mi"
        cpu: "250m"
      limits:
        memory: "512Mi"
        cpu: "500m"</code></pre>
          
          <h3>Monitoring and Observability</h3>
          <p>Implement comprehensive monitoring with Prometheus, Grafana, and centralized logging to maintain visibility into your cluster's health and performance.</p>
          
          <h3>Backup and Disaster Recovery</h3>
          <p>Regular backups of etcd and persistent volumes are crucial for disaster recovery. Consider using tools like Velero for comprehensive backup solutions.</p>
        </div>
      `
    },
    'cicd-pipeline-optimization': {
      title: 'Optimizing CI/CD Pipelines',
      content: `
        <div class="prose prose-lg max-w-none">
          <h3>Pipeline Optimization Strategies</h3>
          <p>Efficient CI/CD pipelines are crucial for rapid software delivery. Here are proven strategies to optimize your pipelines.</p>
          
          <h3>Build Time Optimization</h3>
          <ul>
            <li><strong>Parallel Execution:</strong> Run independent jobs concurrently</li>
            <li><strong>Caching:</strong> Cache dependencies and build artifacts</li>
            <li><strong>Docker Layer Caching:</strong> Optimize Dockerfile for better layer reuse</li>
            <li><strong>Incremental Builds:</strong> Only rebuild changed components</li>
          </ul>
          
          <h3>Example Jenkins Pipeline</h3>
          <pre><code>pipeline {
    agent any
    stages {
        stage('Build') {
            parallel {
                stage('Frontend') {
                    steps {
                        sh 'npm install && npm run build'
                    }
                }
                stage('Backend') {
                    steps {
                        sh 'mvn clean package'
                    }
                }
            }
        }
        stage('Test') {
            steps {
                sh 'npm test && mvn test'
            }
        }
        stage('Deploy') {
            steps {
                sh 'kubectl apply -f deployment.yaml'
            }
        }
    }
}</code></pre>
          
          <h3>Quality Gates</h3>
          <p>Implement automated quality checks including code coverage, security scanning, and performance testing to maintain high code quality.</p>
          
          <h3>Deployment Strategies</h3>
          <p>Use blue-green or canary deployments to minimize downtime and reduce deployment risks.</p>
        </div>
      `
    },
    'infrastructure-as-code': {
      title: 'Infrastructure as Code with Terraform',
      content: `
        <div class="prose prose-lg max-w-none">
          <h3>Getting Started with Terraform</h3>
          <p>Infrastructure as Code (IaC) enables you to manage and provision infrastructure through code, making it version-controlled, repeatable, and scalable.</p>
          
          <h3>Terraform Best Practices</h3>
          <ul>
            <li><strong>Module Organization:</strong> Create reusable modules for common infrastructure patterns</li>
            <li><strong>State Management:</strong> Use remote state storage with locking</li>
            <li><strong>Variable Management:</strong> Use variables and locals for flexibility</li>
            <li><strong>Resource Naming:</strong> Follow consistent naming conventions</li>
          </ul>
          
          <h3>Example AWS Infrastructure</h3>
          <pre><code>resource "aws_vpc" "main" {
  cidr_block           = var.vpc_cidr
  enable_dns_hostnames = true
  enable_dns_support   = true
  
  tags = {
    Name = "${var.project_name}-vpc"
  }
}

resource "aws_subnet" "public" {
  count             = length(var.public_subnet_cidrs)
  vpc_id            = aws_vpc.main.id
  cidr_block        = var.public_subnet_cidrs[count.index]
  availability_zone = data.aws_availability_zones.available.names[count.index]
  
  map_public_ip_on_launch = true
  
  tags = {
    Name = "${var.project_name}-public-${count.index + 1}"
  }
}</code></pre>
          
          <h3>State Management</h3>
          <p>Always use remote state storage with S3 and DynamoDB for state locking in production environments.</p>
          
          <h3>Testing Infrastructure</h3>
          <p>Use tools like Terratest for automated testing of your infrastructure code to ensure reliability and prevent regressions.</p>
        </div>
      `
    }
  };

  function openArticle(articleId) {
    const article = articles[articleId];
    if (article) {
      document.getElementById('modal-title').textContent = article.title;
      document.getElementById('modal-content').innerHTML = article.content;
      document.getElementById('blog-modal').classList.remove('hidden');
      document.body.style.overflow = 'hidden';
    }
  }

  function closeBlogModal() {
    document.getElementById('blog-modal').classList.add('hidden');
    document.body.style.overflow = 'auto';
  }

  // Close modal when clicking outside
  document.getElementById('blog-modal').addEventListener('click', function(e) {
    if (e.target === this) {
      closeBlogModal();
    }
  });
</script>
