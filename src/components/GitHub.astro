---
---

<section id="github" class="py-20 transition-colors duration-300" :class="lightMode ? 'bg-white' : 'bg-black'">
  <div class="container mx-auto px-6">
    <div class="text-center mb-16 animate-slide-up">
      <h2 class="text-4xl font-bold mb-4 transition-colors duration-300" :class="lightMode ? 'text-gray-900' : 'text-white'">GitHub Profile</h2>
      <p class="text-lg transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-300'">My open source contributions and repositories</p>
    </div>
    
    <!-- GitHub Stats -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12 max-w-4xl mx-auto">
      <div class="border rounded-xl p-6 text-center shadow-lg hover:shadow-xl transition-all duration-300" :class="lightMode ? 'bg-white border-gray-200' : 'bg-gray-900 border-gray-800'">
        <h3 id="repo-count" class="text-3xl font-bold text-blue-400 mb-2">-</h3>
        <p class="transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-300'">Repositories</p>
      </div>
      <div class="border rounded-xl p-6 text-center shadow-lg hover:shadow-xl transition-all duration-300" :class="lightMode ? 'bg-white border-gray-200' : 'bg-gray-900 border-gray-800'">
        <h3 id="follower-count" class="text-3xl font-bold text-blue-400 mb-2">-</h3>
        <p class="transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-300'">Followers</p>
      </div>
      <div class="border rounded-xl p-6 text-center shadow-lg hover:shadow-xl transition-all duration-300" :class="lightMode ? 'bg-white border-gray-200' : 'bg-gray-900 border-gray-800'">
        <h3 id="following-count" class="text-3xl font-bold text-blue-400 mb-2">-</h3>
        <p class="transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-300'">Following</p>
      </div>
    </div>
    
    <!-- GitHub README -->
    <div class="max-w-6xl mx-auto mb-12">
      <div class="border rounded-xl p-8 shadow-lg transition-colors duration-300" :class="lightMode ? 'bg-white border-gray-200' : 'bg-gray-900 border-gray-800'">
        <h3 class="text-2xl font-bold mb-6 flex items-center transition-colors duration-300" :class="lightMode ? 'text-gray-900' : 'text-white'">
          📋 GitHub Profile README
        </h3>
        <div id="github-readme" class="prose prose-lg max-w-none">
          <div class="text-center py-8">
            <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-400 mx-auto"></div>
            <p class="mt-4 transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-300'">Loading GitHub profile...</p>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Pinned Repositories -->
    <div class="max-w-6xl mx-auto">
      <h3 class="text-2xl font-bold mb-8 text-center transition-colors duration-300" :class="lightMode ? 'text-gray-900' : 'text-white'">
        📌 Featured Repositories
      </h3>
      <div id="repos-list" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <div class="border rounded-xl p-6 shadow-lg animate-pulse transition-colors duration-300" :class="lightMode ? 'bg-white border-gray-200' : 'bg-gray-900 border-gray-800'">
          <div class="h-4 rounded mb-4 transition-colors duration-300" :class="lightMode ? 'bg-gray-200' : 'bg-gray-600'"></div>
          <div class="h-3 rounded mb-2 transition-colors duration-300" :class="lightMode ? 'bg-gray-200' : 'bg-gray-600'"></div>
          <div class="h-3 rounded mb-4 transition-colors duration-300" :class="lightMode ? 'bg-gray-200' : 'bg-gray-600'"></div>
          <div class="flex space-x-2">
            <div class="h-6 w-12 rounded-full transition-colors duration-300" :class="lightMode ? 'bg-gray-200' : 'bg-gray-600'"></div>
            <div class="h-6 w-12 rounded-full transition-colors duration-300" :class="lightMode ? 'bg-gray-200' : 'bg-gray-600'"></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .repo-card {
    @apply border rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1;
  }
  
  .light .repo-card {
    @apply bg-white border-gray-200;
  }
  
  :not(.light) .repo-card {
    @apply bg-gray-900 border-gray-800;
  }
  
  .repo-card h5 {
    @apply text-lg font-semibold mb-3;
  }
  
  .light .repo-card h5 {
    @apply text-gray-900;
  }
  
  :not(.light) .repo-card h5 {
    @apply text-white;
  }
  
  .repo-card h5 a {
    @apply text-blue-400 hover:text-blue-300 transition-colors;
  }
  
  .repo-card p {
    @apply mb-4 text-sm leading-relaxed;
  }
  
  .light .repo-card p {
    @apply text-gray-600;
  }
  
  :not(.light) .repo-card p {
    @apply text-gray-300;
  }
  
  .repo-stats {
    @apply flex flex-wrap gap-3 text-sm;
  }
  
  .light .repo-stats {
    @apply text-gray-500;
  }
  
  :not(.light) .repo-stats {
    @apply text-gray-400;
  }
  
  .repo-stats span {
    @apply flex items-center gap-1;
  }
  
  #github-readme h1 {
    @apply text-3xl font-bold text-center mb-6 text-blue-400;
  }
  
  #github-readme h3 {
    @apply text-xl font-semibold mb-4;
  }
  
  .light #github-readme h3 {
    @apply text-gray-900 !important;
  }
  
  :not(.light) #github-readme h3 {
    @apply text-white !important;
  }
  
  #github-readme p {
    @apply mb-4;
  }
  
  .light #github-readme p {
    @apply text-gray-700 !important;
  }
  
  :not(.light) #github-readme p {
    @apply text-gray-300 !important;
  }
  
  #github-readme strong {
    @apply font-semibold;
  }
  
  .light #github-readme strong {
    @apply text-gray-900 !important;
  }
  
  :not(.light) #github-readme strong {
    @apply text-white !important;
  }
  
  #github-readme img {
    @apply max-w-full h-auto rounded-lg;
  }
  
  #github-readme a {
    @apply text-blue-400 hover:underline;
  }
  
  #github-readme ul {
    @apply list-disc list-inside mb-4;
  }
  
  .light #github-readme ul {
    @apply text-gray-700 !important;
  }
  
  :not(.light) #github-readme ul {
    @apply text-gray-300 !important;
  }
  
  #github-readme li {
    @apply mb-2;
  }
  
  .light #github-readme li {
    @apply text-gray-700 !important;
  }
  
  :not(.light) #github-readme li {
    @apply text-gray-300 !important;
  }
  
  #github-readme blockquote {
    @apply border-l-4 border-blue-400 pl-4 italic p-4 rounded-r-lg;
  }
  
  .light #github-readme blockquote {
    @apply text-gray-700 bg-gray-100 !important;
  }
  
  :not(.light) #github-readme blockquote {
    @apply text-gray-400 bg-gray-800 !important;
  }
  
  .readme-text {
    color: inherit !important;
  }
  
  .light .readme-text {
    @apply text-gray-700 !important;
  }
  
  :not(.light) .readme-text {
    @apply text-gray-300 !important;
  }
  
  .readme-strong {
    font-weight: 600 !important;
  }
  
  .light .readme-strong {
    @apply text-gray-900 !important;
  }
  
  :not(.light) .readme-strong {
    @apply text-white !important;
  }
  
  .readme-heading {
    font-weight: 600 !important;
  }
  
  .light .readme-heading {
    @apply text-gray-900 !important;
  }
  
  :not(.light) .readme-heading {
    @apply text-white !important;
  }
  
  .readme-quote {
    font-style: italic !important;
  }
  
  .light .readme-quote {
    @apply text-gray-700 !important;
  }
  
  :not(.light) .readme-quote {
    @apply text-gray-400 !important;
  }
</style>

<script>
  const GITHUB_USERNAME = 'sonbarse17';
  const GITHUB_API = `https://api.github.com/users/${GITHUB_USERNAME}`;
  const GITHUB_REPOS_API = `https://api.github.com/users/${GITHUB_USERNAME}/repos?sort=updated&per_page=100`;

  async function fetchGitHubData() {
    try {
      const userResponse = await fetch(GITHUB_API);
      const userData = await userResponse.json();
      
      document.getElementById('repo-count').textContent = userData.public_repos;
      document.getElementById('follower-count').textContent = userData.followers;
      document.getElementById('following-count').textContent = userData.following;
      
      const reposResponse = await fetch(GITHUB_REPOS_API);
      const allRepos = await reposResponse.json();
      
      // Get pinned repositories (fallback to top repos if no pinned)
      const pinnedRepos = [
        'sushant-portfolio',
        'DevOps-Projects',
        'AWS-Infrastructure',
        'Kubernetes-Deployment',
        'CI-CD-Pipeline',
        'Terraform-Modules'
      ];
      
      const filteredRepos = allRepos
        .filter(repo => !repo.fork && pinnedRepos.includes(repo.name))
        .concat(
          allRepos
            .filter(repo => !repo.fork && !pinnedRepos.includes(repo.name))
            .sort((a, b) => b.stargazers_count - a.stargazers_count)
        )
        .slice(0, 6);
      
      const reposList = document.getElementById('repos-list');
      reposList.innerHTML = filteredRepos.map(repo => `
        <div class="repo-card">
          <h5><a href="${repo.html_url}" target="_blank">${repo.name}</a></h5>
          <p>${repo.description || 'No description available'}</p>
          <div class="repo-stats">
            <span>⭐ ${repo.stargazers_count}</span>
            <span>🍴 ${repo.forks_count}</span>
            ${repo.language ? `<span>💻 ${repo.language}</span>` : ''}
          </div>
        </div>
      `).join('');
      
      loadGitHubReadme();
      
    } catch (error) {
      console.error('Error fetching GitHub data:', error);
      document.getElementById('repo-count').textContent = 'Error';
      document.getElementById('follower-count').textContent = 'Error';
      document.getElementById('following-count').textContent = 'Error';
    }
  }

  function loadGitHubReadme() {
    const readmeContent = `
      <div class="text-center mb-8">
        <img src="/sushant-portfolio/images/banner.png" alt="DevOps Banner" class="mx-auto rounded-lg shadow-lg mb-6 max-w-full h-auto">
        <h1>Hey Everyone 👋, I'm Sushant Sonbarse</h1>
        <h3 class="text-xl mb-6 readme-text">A passionate DevOps Engineer from India. I work in the Corporate IT Sector</h3>
        
        <div class="flex justify-center space-x-4 mb-8">
          <a href="https://github.com/sonbarse17" target="_blank">
            <img src="https://img.shields.io/github/followers/sonbarse17?label=Follow&style=social" alt="GitHub Followers">
          </a>
          <a href="https://linkedin.com/in/sushant-sonbarse" target="_blank">
            <img src="https://img.shields.io/badge/LinkedIn-Sushant%20Sonbarse-blue?logo=linkedin&style=flat-square" alt="LinkedIn">
          </a>
        </div>
        
        <img src="https://komarev.com/ghpvc/?username=sonbarse17&label=Profile%20views&color=0e75b6&style=flat" alt="Profile Views" class="mx-auto">
      </div>
      
      <div class="grid md:grid-cols-2 gap-8 mb-8">
        <div>
          <ul class="space-y-2">
            <li class="readme-text">👨‍💻 All of my projects are available at <a href="https://github.com/sonbarse17" target="_blank">GitHub</a></li>
            <li class="readme-text">💬 Ask me about <strong class="readme-strong">DevOps & Cloud DevOps</strong></li>
            <li class="readme-text">📫 How to reach me <strong class="readme-strong">sushantsonbarse07@gmail.com</strong></li>
          </ul>
        </div>
        <div>
          <img src="https://raw.githubusercontent.com/devSouvik/devSouvik/master/gif3.gif" alt="Coding Animation" class="w-full max-w-sm mx-auto">
        </div>
      </div>
      
      <div class="grid md:grid-cols-2 gap-8 mb-8">
        <div class="text-center">
          <img src="https://github-readme-stats.vercel.app/api/top-langs?username=sonbarse17&show_icons=true&locale=en&layout=compact&theme=vue&hide_border=true" alt="Top Languages" class="mx-auto">
        </div>
        <div class="text-center">
          <img src="https://github-readme-stats.vercel.app/api?username=sonbarse17&show_icons=true&locale=en&theme=vue&hide_border=true" alt="GitHub Stats" class="mx-auto">
        </div>
      </div>
      
      <div class="text-center">
        <h3 class="text-xl font-semibold mb-4 readme-heading">👨‍💼 About Me & 🤝 Open to Collaborations</h3>
        <p class="mb-4 readme-text">🤝 Open to <strong class="readme-strong">Project Collaborations</strong><br>
        💼 Offering <strong class="readme-strong">DevOps Consulting / Mentorship</strong><br>
        📧 Let's chat: <a href="mailto:sushantsonbarse07@gmail.com">sushantsonbarse07@gmail.com</a></p>
        
        <blockquote class="text-lg readme-quote">
          "Helping people crack DevOps with real-world knowledge. Let's build and automate the future, one pipeline at a time!"
        </blockquote>
      </div>
    `;
    
    document.getElementById('github-readme').innerHTML = readmeContent;
  }

  document.addEventListener('DOMContentLoaded', fetchGitHubData);
</script>