---
---

<section id="about" class="py-20 transition-colors duration-300" :class="lightMode ? 'bg-gray-50' : 'bg-gray-900'">
  <div class="container mx-auto px-4 sm:px-6">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-16 animate-slide-up">
        <h2 class="text-3xl sm:text-4xl font-bold mb-8 transition-colors duration-300" :class="lightMode ? 'text-gray-900' : 'text-white'">About Me</h2>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <!-- Text Content -->
        <div class="animate-slide-up">
          <p class="text-lg sm:text-xl mb-6 leading-relaxed transition-colors duration-300" :class="lightMode ? 'text-gray-700' : 'text-gray-300'">
            DevOps Engineer specializing in cloud infrastructure, automation, and CI/CD pipelines. 
            Skilled in AWS services, containerization with Docker and Kubernetes, and Infrastructure as Code using Terraform. 
            Passionate about optimizing deployment processes and building reliable, scalable systems.
          </p>

          <!-- Quick Stats -->
          <div class="grid grid-cols-2 gap-6 mb-8">
            <div class="text-center p-6 rounded-xl border transition-colors duration-300" :class="lightMode ? 'bg-white border-gray-200 shadow-lg' : 'bg-gray-800 border-gray-700'">
              <div class="text-3xl font-bold text-blue-500 mb-2">1+</div>
              <div class="text-sm transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-400'">Years Experience</div>
            </div>
            <div class="text-center p-6 rounded-xl border transition-colors duration-300" :class="lightMode ? 'bg-white border-gray-200 shadow-lg' : 'bg-gray-800 border-gray-700'">
              <div class="text-3xl font-bold text-green-500 mb-2">15+</div>
              <div class="text-sm transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-400'">Technologies</div>
            </div>
          </div>
        </div>

        <!-- Visual Element -->
        <div class="animate-slide-up" style="animation-delay: 0.2s">
          <div class="relative">
            <!-- DevOps Workflow Diagram -->
            <div class="rounded-2xl p-8 border transition-colors duration-300" :class="lightMode ? 'bg-white border-gray-200 shadow-xl' : 'bg-gray-800 border-gray-700'">
              <div class="text-center mb-6">
                <h3 class="text-xl font-bold transition-colors duration-300" :class="lightMode ? 'text-gray-900' : 'text-white'">DevOps Workflow</h3>
              </div>

              <!-- Circular workflow -->
              <div class="relative w-64 h-64 mx-auto">
                <div class="absolute inset-0 rounded-full border-4 border-dashed animate-spin-slow" :class="lightMode ? 'border-blue-300' : 'border-blue-500/50'"></div>

                <!-- Workflow steps -->
                <div class="absolute top-0 left-1/2 transform -translate-x-1/2 -translate-y-1/2">
                  <div class="w-12 h-12 rounded-full flex items-center justify-center text-white font-bold bg-blue-500">1</div>
                  <div class="text-xs mt-2 text-center transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-400'">Plan</div>
                </div>

                <div class="absolute top-1/2 right-0 transform translate-x-1/2 -translate-y-1/2">
                  <div class="w-12 h-12 rounded-full flex items-center justify-center text-white font-bold bg-green-500">2</div>
                  <div class="text-xs mt-2 text-center transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-400'">Code</div>
                </div>

                <div class="absolute bottom-0 left-1/2 transform -translate-x-1/2 translate-y-1/2">
                  <div class="w-12 h-12 rounded-full flex items-center justify-center text-white font-bold bg-purple-500">3</div>
                  <div class="text-xs mt-2 text-center transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-400'">Deploy</div>
                </div>

                <div class="absolute top-1/2 left-0 transform -translate-x-1/2 -translate-y-1/2">
                  <div class="w-12 h-12 rounded-full flex items-center justify-center text-white font-bold bg-orange-500">4</div>
                  <div class="text-xs mt-2 text-center transition-colors duration-300" :class="lightMode ? 'text-gray-600' : 'text-gray-400'">Monitor</div>
                </div>

                <!-- Center logo -->
                <div class="absolute inset-0 flex items-center justify-center">
                  <div class="w-16 h-16 rounded-full flex items-center justify-center transition-colors duration-300" :class="lightMode ? 'bg-gray-100' : 'bg-gray-700'">
                    <svg class="w-8 h-8 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                    </svg>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>